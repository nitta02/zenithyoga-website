const menuButton=document.querySelector('.program-menu-toggle');const menu=document.querySelector('#program-menu');menuButton?.addEventListener('click',()=>{const open=menu.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));});document.querySelectorAll('.filters button').forEach(button=>button.addEventListener('click',()=>{document.querySelector('.filters .selected')?.classList.remove('selected');button.classList.add('selected');}));const newsletter=document.querySelector('.newsletter');newsletter?.addEventListener('submit',event=>{event.preventDefault();const input=newsletter.querySelector('input');if(!input.value.trim())return;input.value='';input.placeholder='Thanks for subscribing';});
